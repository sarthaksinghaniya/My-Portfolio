[build]
  command = "npm run build"
  publish = "dist"

[dev]
  framework = "vite"

# SPA-style routing (serves index.html for all routes)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
